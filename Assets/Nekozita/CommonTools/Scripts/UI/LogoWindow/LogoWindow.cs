using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/*
public class LogoWindow : WindowBase
{

    LogoWindowView View;

    protected override void SettingView()
    {
        base.SettingView();

        View = m_View as LogoWindowView;

        Init();
    }

    private void Init()
    {
        // もしバックグラウンドが非表示なら表示させる
        if (!View.Blocker.gameObject.activeInHierarchy) View.Blocker.gameObject.SetActive(true);

        // FadeImageの色情報(アルファ値を含む)を取得
        View.SetFadeImageColor();

        // フェードインをスタートさせる
        View.FadeInFlag = true;
    }
}
*/